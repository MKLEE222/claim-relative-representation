# Mediation synthesis v1

Date: 2026-09-24

## Result

The access layer no longer supports a one-policy retrieval story.

Three source-verified Yule-Cordier carriers require three different native mediation routes.

### Paper money

Development result:
- later evidence absent from current-object scope;
- broad collection scope alone insufficient;
- sequence-aware navigation plus seed-side source/domain chaining sharply reduces burden.

Primary supported mediators:
M_CITED_SOURCE + M_NATIVE_SEQUENCE.

### Kinsay

Generic seed/task retrieval failed:
- later-layer target rank 304 / 482.

A source-side identity note provides:
Lin-ngan <-> Hang-chau.

After a rule-preserving parser repair, native referent mediation moves the target to rank 4 / 482.

Primary supported mediators:
M_REFERENT_ALIAS + M_NATIVE_SEQUENCE.

This result is exploratory because the alias mechanism was diagnosed after the generic failure.

### Coal

M_LOCAL_ATTACHMENT is directly support-feasible:

- focal span: target outside scope;
- local linear search: rank 4 / 6;
- native note attachment: rank 1 / 1.

Primary supported mediator:
M_LOCAL_ATTACHMENT.

## Two-stage access model

The access object is now:

\[
Support(m\mid tau,R,K)
\rightarrow
Compose(m,\pi)
\rightarrow
Retrievability
\]

rather than:

\[
text\ similarity
\rightarrow
retrieval.
\]

Support feasibility is evaluated before downstream retrieval effect.

A missing mediator is not interpreted as a difficult mediator; the primitive is simply not instantiated under that contract.

## Carrier ladder

The evidence also motivates a staged distinction:

\[
HELD
\rightarrow
ACCESSIBLE
\rightarrow
RECOVERABLE
\rightarrow
COMPOSABLE
\rightarrow
WARRANT\text{-}AUTHORIZED.
\]

Current access experiments establish only the first four stages.

WARRANT-AUTHORIZED remains a later humanistic adjudication state.

## Relation to prior project assets

The two-stage gate, fixed-target discipline, source binding, partial-state reporting, and separate evidence authorities are reused methodological disciplines from prior work.

The DH-specific scientific object is not those generic principles.

It is the source-grounded scholarly mediation layer and its empirically differentiated carrier types across real editorial objects.

## Next falsification

A strong next test should select a new episode for an independent scientific reason and freeze its eligible mediation primitives before retrieval.

The key question is no longer whether "the method retrieves the target."

It is whether the support gate chooses the right kind of documentary bridge without outcome-driven rescue.
